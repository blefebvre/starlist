extends layout

block content
	h1= title
	- each item in list.items
		div.item
			div.content= item.content
	form( method="post" )
		div
			input( type="hidden", name="_id", value=list._id.toHexString())
			label(for="content") New: 
			input(type="text", name="content")
		div
			input(type="submit", value="Add")

	a(href='/') &lt; Back